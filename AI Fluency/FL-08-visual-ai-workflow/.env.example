# Gemini is used first when both providers are configured.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Optional fallback provider. The OpenAI SDK remains installed for the assignment setup requirement.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
