import "./globals.css";

export const metadata = {
  title: "Decision Flow Studio",
  description: "Visual YES/NO AI workflows powered by Inngest.",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
